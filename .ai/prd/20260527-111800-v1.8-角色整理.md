# PRD: v1.8「角色整理」

> Generated: 2026-05-27 11:18:00
> Type: Feature | Priority: P3 | Est: 20h

## 1. 目标用户/场景

**目标用户**: 角色数量较多，需要分类整理的用户
**使用场景**:
- 用户创建多个角色后，按分组管理
- 用户按标签筛选角色
- 用户批量导出/删除角色

## 2. 功能列表

### MVP 功能

#### 角色分组/文件夹
- [ ] 创建/编辑/删除分组
- [ ] 拖拽角色到分组
- [ ] 分组内角色排序
- [ ] 分组折叠/展开

#### 标签管理页
- [ ] 标签列表页
- [ ] 创建/编辑/删除标签
- [ ] 标签颜色自定义
- [ ] 查看使用某标签的所有角色

#### 角色排序
- [ ] 最近聊天排序
- [ ] 创建时间排序
- [ ] 名称字母排序
- [ ] 记住用户排序偏好

#### 批量操作
- [ ] 多选角色
- [ ] 批量导出选中角色
- [ ] 批量删除选中角色（需确认）

## 3. 数据模型

```kotlin
@Entity(tableName = "character_groups")
data class CharacterGroup(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    val color: String = "#007AFF",
    val sortOrder: Int = 0
)

// Character 新增字段
@Entity(tableName = "characters")
data class Character(
    // ... existing fields
    val groupId: Long? = null  // 分组 ID
)
```

## 4. 用户旅程

1. 用户在角色列表页点击「排序」
2. 选择排序方式
3. 列表刷新为新顺序

1. 用户长按角色
2. 选择「移动到分组」
3. 选择目标分组
4. 角色移动成功

## 5. 非功能需求

- **性能**: 100 个角色内流畅操作
- **兼容性**: Android 8.0+

---

## 验收标准

- [ ] 可创建/编辑/删除角色分组
- [ ] 可拖拽角色到分组
- [ ] 可管理标签
- [ ] 支持多种排序方式
- [ ] 支持批量导出/删除角色

---
*Generated based on current codebase + monthly iteration plan v1.8*