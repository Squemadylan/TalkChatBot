<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    
    <!-- 推送相关权限 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:forceDarkAllowed="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.memory.MemoryViewerActivity"
            android:exported="false"
            android:label="4 层记忆查看"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" />
        
        <!-- ============================================ -->
        <!-- 中国手机品牌推送 SDK 配置 -->
        <!-- ============================================ -->
        
        <!-- 小米推送配置 -->
        <!-- TODO: 添加实际的小米AppId和AppKey -->
        <!--
        <meta-data
            android:name="MIPUSH_APPID"
            android:value="your_xiaomi_app_id" />
        <meta-data
            android:name="MIPUSH_APPKEY"
            android:value="your_xiaomi_app_key" />
        -->
        
        <!-- 华为 HMS Push 配置 -->
        <!-- TODO: 添加华为AppGallery Connect的appid -->
        <!--
        <meta-data
            android:name="com.huawei.hms.client.appid"
            android:value="appid=your_huawei_app_id" />
        -->
        
        <!-- OPPO 推送配置 -->
        <!-- TODO: 添加实际的OPPO AppId和AppKey -->
        <!--
        <meta-data
            android:name="com.heytap.mcp.appid"
            android:value="your_oppo_appid" />
        <meta-data
            android:name="com.heytap.mcp.appkey"
            android:value="your_oppo_appkey" />
        -->
        
        <!-- VIVO 推送配置 -->
        <!-- TODO: 添加实际的VIVO AppId和AppKey -->
        <!--
        <meta-data
            android:name="com.vivo.push.app_id"
            android:value="your_vivo_appid" />
        <meta-data
            android:name="com.vivo.push.app_key"
            android:value="your_vivo_appkey" />
        -->
        
        <!-- 荣耀推送配置 -->
        <!-- TODO: 添加实际的荣耀AppId和AppKey -->
        <!--
        <meta-data
            android:name="com.hihonor.push.appid"
            android:value="your_honor_appid" />
        -->
        
        <!-- 推送接收器 - 华为 -->
        <!-- TODO: 根据实际SDK添加Receiver -->
        <!--
        <receiver android:name=".push.HuaweiPushReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.huawei.push.action.MESSAGING_EVENT" />
            </intent-filter>
        </receiver>
        -->
        
        <!-- 推送接收器 - 小米 -->
        <!-- TODO: 根据实际SDK添加Receiver -->
        <!--
        <receiver android:name=".push.XiaomiPushReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.xiaomi.push.NOTIFICATION_ARRIVED" />
                <action android:name="com.xiaomi.push.MESSAGING_EVENT" />
            </intent-filter>
        </receiver>
        -->
        
    </application>

</manifest>
